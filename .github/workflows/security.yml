name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Weekly, so advisories surface without waiting for a change.
    - cron: "0 9 * * 1"

permissions:
  contents: read

jobs:
  # Audits the locked versions of everything the zipy image installs.
  audit:
    name: Python audit
    runs-on: ubuntu-latest
    # paths-filter lists a pull request's files through the API.
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: dorny/paths-filter@v4.0.3
        id: filter
        with:
          filters: |
            deps:
              - pyproject.toml
              - uv.lock
              - apps/*/pyproject.toml
              - .github/workflows/security.yml
      - if: github.event_name == 'schedule' || steps.filter.outputs.deps == 'true'
        uses: astral-sh/setup-uv@v10.1.0
      - if: github.event_name == 'schedule' || steps.filter.outputs.deps == 'true'
        run: |
          uv export --locked --no-dev --no-emit-workspace --no-hashes --all-packages \
            > "$RUNNER_TEMP/requirements.txt"
          uvx pip-audit --strict --progress-spinner off --disable-pip --no-deps \
            -r "$RUNNER_TEMP/requirements.txt"

  # Audits the website's production dependencies.
  npm:
    name: Website audit
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: dorny/paths-filter@v4.0.3
        id: filter
        with:
          filters: |
            deps:
              - apps/website/package.json
              - apps/website/package-lock.json
              - .github/workflows/security.yml
      - if: github.event_name == 'schedule' || steps.filter.outputs.deps == 'true'
        uses: actions/setup-node@v7.0.0
        with:
          node-version: 22
      - if: github.event_name == 'schedule' || steps.filter.outputs.deps == 'true'
        run: cd apps/website && npm audit --omit=dev --audit-level=high

  # New dependencies on a pull request with known vulnerabilities or a license outside the list.
  review:
    name: Dependency review
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/dependency-review-action@v5.0.0
        with:
          fail-on-severity: high
          comment-summary-in-pr: on-failure

  # The gitleaks CLI, verified against the release checksums. The gitleaks action needs a
  # paid license on organization repositories; the CLI does not.
  secrets:
    name: Secret scan
    runs-on: ubuntu-latest
    env:
      GITLEAKS_VERSION: 8.30.1
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0
      - name: Install gitleaks
        working-directory: ${{ runner.temp }}
        run: |
          base="https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}"
          archive="gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz"
          curl -sSfLO "$base/$archive"
          curl -sSfL "$base/gitleaks_${GITLEAKS_VERSION}_checksums.txt" | grep " $archive\$" | sha256sum -c -
          tar -xzf "$archive" gitleaks
          echo "$RUNNER_TEMP" >> "$GITHUB_PATH"
      - run: gitleaks git --redact --verbose --exit-code 1 .
