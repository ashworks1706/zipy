// Zipy SDK - Edge kernel for vector databases

/**
 * Minimal placeholder function
 * @returns {string} Package name
 */
function hello() {
  return "Zipy edge kernel SDK";
}

module.exports = { hello };
